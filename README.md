# QOI Encoder

To run the program,
Just run main.py (with python3)

Enter the path to the image and the final qoi format image (w/ or w/o extension)

Linux:
`$ python3 main.py [png] [qoi]`

Windows:
You can probably just double click main.py if you have Python installed.
